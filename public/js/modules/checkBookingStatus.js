import showModal from './showModal.js';

// Pop the modal

// Tell user what needs be done
const checkBookingStatus = function (modalLayout) {
  // Get the modal layout
  showModal(modalLayout);
};

export default checkBookingStatus;
